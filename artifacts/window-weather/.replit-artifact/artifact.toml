kind = "web"
previewPath = "/"
title = "Window Weather Notifier"
version = "1.0.0"
id = "artifacts/window-weather"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18961

[services.development]
run = "pnpm --filter @workspace/window-weather run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/window-weather", "run", "build" ]
publicDir = "artifacts/window-weather/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18961"
BASE_PATH = "/"
